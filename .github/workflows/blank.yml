name: CI

on:
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:
env: 
  global_env: global

jobs:    
  build_unix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: check tools
        run: |
          cmake --version
          
